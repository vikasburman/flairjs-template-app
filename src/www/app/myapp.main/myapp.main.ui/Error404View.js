const { VueView } = ns('flair.ui.vue');

/**
 * @name Error404View
 * @description Default Error View
 */
$$('ns', '(auto)');
Class('(auto)', VueView, function() {
    $$('protected');
    this.data = null;

    $$('protected');
    this.layout = null;
});
