const { VueView } = ns('flair.ui.vue');

/**
 * @name HomeView
 * @description Default Home View
 */
$$('ns', '(auto)');
Class('(auto)', VueView, function() {
    $$('protected');
    this.data = null;

    $$('protected');
    this.layout = null;
});
