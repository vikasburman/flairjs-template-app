define([
    use('[Base]')
], (Base) => {
    /**
     * @class app.core.comm.Service
     * @classdesc app.core.comm.Service
     * @desc Service base.
     */    
    return Class('app.core.comm.Service', Base, function(attr) {
    });
});