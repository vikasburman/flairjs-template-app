define(() => {
    return class Base {

        // get value of given object's nested path --- mock version
        value(path, defaultValue = null) {
            return defaultValue;
        };
    };
});