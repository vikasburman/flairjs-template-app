define([
    '[Module]'
], (Module) => {
    return class Bootware extends Module {

        // boot operation (sync/async)
        boot() {};

        // ready operation (sync/async)
        ready() {};
    };
});